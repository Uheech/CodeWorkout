def solution(a, b):
# 기약분수가 아닐 때 > 분자/분모에 최대공약수가 있을때
    for i in range(1,b):
        if (a%i==0) and (b%i==0):
# 기약분수로 만들어주기> 분자/분모를 최대공약수로 나눠주기
            a=a//i
            b=b//i

# 기약분수일 때
        else:
# 분모의 소인수가 2,5만 존재한다면
            if b%2==0 or b%5==0:
# 유한소수이다.
                return 1
            else:
                return 2
